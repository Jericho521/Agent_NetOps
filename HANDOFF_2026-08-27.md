# NetOps 平台 — 交接文档（面向新 PC / 新 CodeBuddy 无缝接手）

> 最后更新：**2026-08-28**
> 仓库地址：**https://github.com/Jericho521/Agent_NetOps.git**（Public）
> 项目根目录：`Agent_NetOps/netops-platform/`
> 本文件 = 环境速查 + 启动指南 + 关键约定 + 最近改动记录。
> 详尽的功能开发历史见同目录 `HANDOFF_2026-08-25.md` 与 `NetOps平台开发进度.md`。

---

## 一、环境依赖（新 PC 必须安装）

| 依赖 | 版本要求 | 安装方式 |
|------|----------|----------|
| **Git** | 任意新版 | https://git-scm.com/download/win ，装完需重启终端 |
| **Node.js + npm** | Node ≥ 18（建议 20 LTS） | https://nodejs.org 装 LTS |
| **Python** | 3.9 ~ 3.11 | https://www.python.org/downloads/ （装时勾 Add to PATH） |
| **VictoriaMetrics** | 单机版 | 后端指标存储，见下方「第四步」 |

> ⚠️ **GitHub 不支持密码登录**：推送必须用 Personal Access Token (PAT)。
> 生成：GitHub → Settings → Developer settings → Personal access tokens → classic，
> 勾 `repo`，过期选 No expiration，生成后复制 `ghp_xxx`。
> 推命令：
> ```powershell
> git remote set-url origin https://<你的TOKEN>@github.com/Jericho521/Agent_NetOps.git
> git push
> ```
> 推完建议把 remote 改回普通地址（去掉 token）避免泄露。

---

## 二、在新 PC 上拉取并跑起来

```powershell
# 1. 克隆
git clone https://github.com/Jericho521/Agent_NetOps.git
cd Agent_NetOps

# 2. 后端：建虚拟环境 + 装依赖 + 建 .env
cd netops-platform\backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt      # 若没有该文件，见下方「依赖清单」手动 pip

# 3. 创建 backend/.env（关键！没有它后端起不来）
#    内容至少含：
#    SECRET_KEY=<任意随机串>
#    CREDENTIALS_KEY=<32字节hex随机串，用于AES加密设备口令>
#    DATABASE_URL=sqlite:///./netops.db
#    VICTORIA_METRICS_URL=http://localhost:8428

# 4. 前端：装依赖
cd ..\frontend
npm install

# 5. 启动顺序：先 VictoriaMetrics → 后端 → 前端
```

---

## 三、启动步骤（日常开发 / 验证）

```powershell
# 终端 A：VictoriaMetrics（指标库，必须先起）
#   下载 victoria-metrics-windows-amd64.exe 后：
cd Agent_NetOps\netops-platform\victoria-metrics
.\victoria-metrics-windows-amd64.exe   # 默认 8428 端口

# 终端 B：后端
cd Agent_NetOps\netops-platform\backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 终端 C：前端
cd Agent_NetOps\netops-platform\frontend
npm run dev        # 开发热更；或 npm run build && 由后端静态托管

# 访问：http://localhost:5173  （Vite 默认）或 http://localhost:8000 （若前端 build 后由后端 serve）
# 默认管理员账号：admin / admin123
```

> 后端端口被占用时：
> `netstat -ano | findstr :8000` → `taskkill /PID <PID> /F`

---

## 四、关键配置 / 环境变量（`.env`，必须）

| 变量 | 说明 |
|------|------|
| `SECRET_KEY` | JWT 签名密钥，任意随机串 |
| `CREDENTIALS_KEY` | 32 字节 hex，AES 加密设备 SNMP 社区字/口令。改了会导致旧凭据解密失败 |
| `DATABASE_URL` | SQLite 默认 `sqlite:///./netops.db` |
| `VICTORIA_METRICS_URL` | 默认 `http://localhost:8428` |

> ⚠️ `.env`、`.venv`、`node_modules`、`netops.db`、`__pycache__`、`victoria-metrics/data` 均已在 `.gitignore` 排除，**不会入库**，新 PC 必须自己重建。

---

## 五、项目结构速览

```
Agent_NetOps/
├─ netops-platform/
│  ├─ backend/                # FastAPI
│  │  ├─ app/
│  │  │  ├─ main.py           # 入口，挂载所有 router
│  │  │  ├─ config.py         # 配置（注意第26行曾因残留"""崩过，勿加）
│  │  │  ├─ models.py         # SQLAlchemy 模型（Device/Link/Alert/AlertRule/TrapLog/TrapRule...）
│  │  │  ├─ schemas.py        # Pydantic
│  │  │  ├─ db.py             # 建表 + 迁移
│  │  │  ├─ collector/        # SNMP 采集 / 拓扑发现 / 调度
│  │  │  ├─ routers/          # 按功能分模块（devices/topology/alerts/traps/regions...）
│  │  │  └─ alerting/         # 告警规则引擎
│  │  └─ requirements.txt
│  ├─ frontend/               # React + Vite + TS
│  │  ├─ src/
│  │  │  ├─ App.tsx           # 路由
│  │  │  ├─ api.ts            # 所有后端接口封装
│  │  │  ├─ components/Layout.tsx  # 侧边栏导航
│  │  │  ├─ pages/            # 每功能一页（Login/DeviceList/Topology/Traps/Alerts...）
│  │  │  └─ index.css         # 全局样式（CSS 变量换肤）
│  │  └─ public/              # 静态资源（logo.ico / logo-512.png）
│  └─ victoria-metrics/       # 时序数据库
├─ HANDOFF_2026-08-25.md      # 详尽的旧功能开发史
├─ HANDOFF_2026-08-27.md      # 本文件
└─ NetOps平台开发进度.md       # 最早的项目计划
```

---

## 六、最近改动记录（2026-08-27 ~ 08-28）

### A. 设备连通性测试自动更新状态（2026-08-27）
- `backend/app/routers/devices.py`：`test_connectivity_stream`（SSE 测试接口）
  - **测试成功 → 设备 `status='online'`**，并自动补全 `model`/`vendor`
  - **测试失败 → 设备 `status='error'`（新增，旧版失败不改状态）**
- 前端 `DeviceList.tsx` 关闭测试弹窗时自动 `loadDevices()` 刷新，状态立即反映。

### B. 登录页美化（2026-08-27）
- `frontend/src/pages/Login.tsx`：改为左右分栏
  - 左 55%：深蓝渐变 + 品牌介绍 + 功能标签 + 装饰元素
  - 右 45%：高清 logo + 登录表单
- 引用 `public/logo-512.png` 作登录页 logo。
- `frontend/src/index.css`：新增 `.feature-tag` 样式、输入框 focus 高亮。
- **登录成功后跳 `/`（仪表盘），不再直接跳 `/devices`**。

### C. Favicon / Logo 高清化（2026-08-27 ~ 28）
- 源图：`D:\Agent_NetOps\logo.png`（8000×8000 高清）。
- 生成：`public/logo.ico`（多尺寸 16~256，从 512 母版降采样，比直缩清晰）+ `public/logo-512.png`（大图用）。
- `frontend/index.html` 已配 `<link rel="icon" href="/logo.ico">`。
- 注意：Windows ICO 格式上限 256×256；512 高清母版单独存 `logo-512.png` 供页面大图。

### D. SNMP Trap 页面上线（2026-08-27）
后端 `trap.py` 早已存在（日志/规则/状态 API），但前端无入口。**本次补齐：**
- `frontend/src/api.ts`：新增 `fetchTrapLogs`/`fetchTrapRules`/`createTrapRule`/`deleteTrapRule`/`fetchTrapStatus` 及类型。
- `frontend/src/pages/Traps.tsx`：**新增**。两个 Tab：
  - Trap 日志（源 IP/版本/PDU 类型/变量绑定可展开）
  - 映射规则（OID 前缀 → 告警级别，可增删）
  - 顶部显示监听状态（UDP 端口，默认 1620）。
- `frontend/src/App.tsx`：加 `/traps` 路由。
- `frontend/src/components/Layout.tsx`：侧边栏加「📡 SNMP Trap」菜单（告警规则下方）。

### E. Git 初始化与多机同步（2026-08-28）
- 仓库：`https://github.com/Jericho521/Agent_NetOps.git`（Public）。
- 已 `git init` + `git add -A` + 首次 commit（"init: NetOps platform v1.0"）。
- `.gitignore` 已写好（排除 node_modules/.env/数据库/__pycache__/dist 等）。
- ⚠️ **代码初次 push 需要你在 GitHub 生成 PAT 后执行 push**（见第一节）。
- 回家后：`git clone` → 按第二、三节装环境跑起来即可继续开发。

---

## 七、关键约定速查（勿破）

| 项 | 约定 |
|----|------|
| 默认管理员 | `admin` / `admin123` |
| 采集间隔 | 默认 60s（`Device.poll_interval`，10~3600） |
| 告警分级 | P0 红 / P1 橙 / P2 黄 / P3 蓝；离线/采集异常=`severity=NULL` 灰标签（**不进 P 级**） |
| 重要链路 | `Link.is_critical`，中断生成 P0，恢复自动 resolve |
| 拓扑自连过滤 | `discover_topology()` 跳过 `remote_sysname == 自身 sys_name` 的邻居（堆叠口自连） |
| SNMP 采集 | 优先 v3，默认 v3；云汉核心需设备端放行 LLDP 子树 `1.0.8802` |
| `_snmp_walk` | **保留 `return_full_oid=True`**，LLDP 三段索引依赖，勿删 |
| 设备凭据 | AES 加密存 `device_credential`；密钥 `CREDENTIALS_KEY` |
| 指标存储 | VictoriaMetrics（先启动再起后端） |

---

## 八、遗留 / 待办（接手后优先）

1. **Git 首次推送到远程**（需 PAT，见第一节）—— 当前本地已 commit，未 push。
2. 自定义视图位置持久化（`customPositionsRef` 仅内存，刷新丢失）。
3. 链路带宽可视化（端口对标签 + 接口利用率着色）。
4. 告警闭环：离线恢复不误关阈值告警。
5. 设备详情接口列表 / 端口利用率。
6. SNMP Trap 联动验证（规则匹配后是否真生成告警）。
7. 区域/子区域实际数据（拓扑筛选依赖）。

---

## 九、给新 CodeBuddy 的一句话总结

> 这是一个 SNMP + LLDP 网络运维平台（华为交换机为主），前后端分离（FastAPI + React/Vite）。
> 已具备：设备管理、拓扑自动发现（力导向/星型/树形/自定义 + 全屏 + 缩放拖拽）、
> 告警分级、区域管理、报表、AI 助手、SNMP Trap 接收。
> 接手时先按第二/三/四节把环境和 `.env` 配好，`uvicorn` + `npm run dev` 即可本地跑。
> 改前端加页面：写 `src/pages/Xxx.tsx` → `App.tsx` 加 Route → `Layout.tsx` 加菜单 → `api.ts` 加接口封装。
