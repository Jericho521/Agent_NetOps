# NetOps 平台 — 交接文档（2026-08-25 ~ 08-26）

> 最后更新：2026-08-26
> 本文件补充 `NetOps平台开发进度.md`（2026-08-24）中**本次会话新增/修改的内容**，旧文件的基础环境、启动步骤仍有效。
> 项目根目录：`d:\Agent_NetOps\netops-platform\`
> 助手：CodeBuddy（历史会话可恢复，本文件为离线交接备份）

---

## 一、本次会话完成的工作（按时间顺序）

### 1. 设备详情曲线（CPU/内存）修复 — 华为 S7700 MIB
参考 PDF `D:\Agent_NetOps\S7700 V200R023C00 MIB参考(pdf)` 确认正确 OID：
- **CPU**：优先 `hwEntityCpuUsage`（`1.3.6.1.4.1.2011.5.25.31.1.1.1.1.5`，WALK，取主控板/最高值）；回退 `hwCpuDevDuty`。
- **内存**：优先 `hwEntityMemUsage`（`...31.1.1.1.1.7`，百分比）；回退 `hwMemoryDevTable`（used/total 计算）。
- **型号**：`entPhysicalDescr` + `sysDescr` 正则提取（之前直接把 sysDescr 整段塞进 model，导致型号为空）。
- **断崖下跌修复**：旧代码取到的 CPU 是低负载子部件（备用主控），改为过滤/取最大值。

代码位置：`backend/app/collector/snmp.py`（`_collect_cpu` / `_collect_mem` / `_extract_model`）。

### 2. 告警分级体系（最终约定，已落地）
**级别：P0 红 / P1 橙 / P2 黄 / P3 蓝；仅离线（灰，不进 P 级别）**

| 场景 | 级别 |
|------|------|
| 堆叠分裂 / M-LAG 脑裂 / 用户标记的重要链路中断 | P0 红 |
| CPU/内存 > 85% | P1 橙 |
| CPU/内存 > 95% | P0 红（严重阈值升级） |
| CPU/内存 其他阈值规则 | 规则自定义（P1/P2/P3） |
| 设备离线 / 采集异常 | **不分级（severity=NULL），仅灰色「离线」标签** |

**改动文件：**
- `backend/app/models.py`：`Alert.severity` 改为 `nullable=True`；`AlertRule` 新增 `critical_threshold` 字段（超过则升级 P0）。
- `backend/app/schemas.py`：`AlertRuleCreate/Update/Response` 加 `critical_threshold`；`AlertResponse.severity` 可空。
- `backend/app/alerting/rules.py`：`_evaluate_rule` 支持 `critical_threshold` → 升级 P0。
- `backend/app/collector/scheduler.py`：`_handle_device_alert` 离线告警 `severity=None`；新增 `_ensure_default_rules`（自动创建 CPU/内存规则：阈值 85=P1，严重阈值 95=P0）。
- `backend/app/db.py`：迁移 alert 表（补 `fired_at/acknowledged_at/resolved_at/acknowledged_by` 列，severity 改可空）、`alert_rule` 加 `critical_threshold` 列。
- `frontend/src/index.css`：severity 颜色 P0红/P1橙/P2黄/P3蓝；离线灰。
- `frontend/src/pages/Alerts.tsx` / `Dashboard.tsx`：`severity=null` 只显示「离线/采集异常」标签。
- `frontend/src/pages/AlertRules.tsx`：加「严重阈值」输入框；级别下拉 P0-P3。
- `frontend/src/api.ts`：`AlertRule.critical_threshold`、`AlertItem.severity: number | null`。

> ⚠️ 曾一度错误地把 P0 删除、P1 当红（用户纠正后已恢复）。**最终以本表为准**。

### 3. 区域 / 子区域管理（前后端均已存在，本次仅补前端类型）
- 后端 `routers/regions.py` + 模型 `Region`/`SubRegion` 已完整（CRUD + 级联删除 + 描述）。
- 前端 `pages/RegionManager.tsx` + 侧边栏入口 + `/regions` 路由已存在。
- 本次修正：`frontend/src/api.ts` 的 `Device` 接口补齐 `region_id/sub_region_id/region_name/sub_region_name`；`fetchDevices` 参数加 `region_id/sub_region_id`（后端早已支持筛选）。
- `index.css` 修掉一个多余的 `}`（build 警告）。
- `npm run build` 现已**干净通过**。

### 5. 重要链路标记功能（2026-08-26 新增）

用户可在拓扑图上将链路标记为「重要链路」；当 LLDP/CDP 邻居消失时自动生成 P0 级告警，恢复后自动关闭。

- `backend/app/models.py`：`Link.is_critical` 字段。
- `backend/app/db.py`：`link` 表迁移加 `is_critical` 列。
- `backend/app/routers/topology.py`：新增 `PUT /api/topology/links/{id}/critical` 标记接口；拓扑返回含 `is_critical` 与 `critical_count` 统计。
- `backend/app/collector/topology.py`：`discover_topology()` 继承旧 critical 标记；检测中断/恢复并生成/关闭 `category='link'`、P0 告警。
- `frontend/src/api.ts`：`TopologyLink.is_critical`、`updateLinkCritical()`。
- `frontend/src/pages/Topology.tsx`：重要链路红色粗线 + ★ 显示；右键菜单/详情面板 ☆/★ 切换；图例与统计更新。
- `frontend/src/pages/Alerts.tsx` / `Dashboard.tsx`：`category='link'` 渲染为「链路」标签。

验证：模拟重要链路丢失 → 生成 P0 告警；恢复 → 告警自动 resolve。

### 6. 拓扑图增强与网元类型匹配（2026-08-26 新增）

解决用户提出的 4 点需求：

1. **网页内全屏**：拓扑容器支持 `requestFullscreen`，按钮「⛶ 全屏 / 退出全屏」。
2. **一键整理布局**：顶部增加「力导向」「星型」「树形」三个按钮。
   - 力导向：原有斥力+弹簧动画。
   - 星型：以度数最大/核心设备为圆心，其他节点均匀分布圆周。
   - 树形：BFS 分层，核心设备在顶层。
3. **区分网元类型**：`Device` 模型新增 `device_type`（single/stack/mlag/cluster）。
   - 单机：圆形
   - 堆叠：圆角矩形
   - M-LAG：菱形
   - 集群：六边形
   - 未知/未录入：虚线圆形
   - 图例同步更新。
4. **修复 "生产汇聚交换机" 与 "DMZ-2-HJ" 不匹配**：`Device` 模型新增 `sys_name`（LLDP/CDP 系统名别名）。
   - 拓扑发现匹配对端时，优先查 `device.name`，再查 `device.sys_name`，最后查 `device.ip`。
   - 用户在「设备管理」/「设备详情」中把"生产汇聚交换机"的系统名填为 `DMZ-2-HJ`，拓扑图即可正确合并为一个节点。

**改动文件**：
- `backend/app/models.py`：`Device.sys_name`、`Device.device_type`。
- `backend/app/db.py`：`device` 表迁移加 `sys_name`、`device_type` 列。
- `backend/app/schemas.py`：`DeviceCreate`/`DeviceUpdate`/`DeviceResponse` 加 `sys_name`、`device_type`。
- `backend/app/routers/devices.py`：创建设备时保存 `sys_name`、`device_type`。
- `backend/app/collector/topology.py`：匹配 remote_sysname 时同时查 `sys_name`。
- `backend/app/routers/topology.py`：节点返回 `sys_name`、`device_type`。
- `frontend/src/api.ts`：`Device`/`TopologyNode` 类型加 `sys_name`、`device_type`。
- `frontend/src/pages/Topology.tsx`：全屏按钮、布局切换、按 `device_type` 渲染不同形状。
- `frontend/src/pages/DeviceList.tsx`：创建设备表单加「系统名」「网元类型」。
- `frontend/src/pages/DeviceDetail.tsx`：详情头部显示「网元类型」「系统名」。

验证：将在线设备 sys_name 设为 DMZ-2-HJ 后运行 `discover_topology`，5 条 DMZ-2-HJ 链路全部正确匹配到该设备，不再显示为未知节点。

### 6.1 修复 "生产汇聚交换机" 重复显示为两个节点（2026-08-26 追加）

**根因**：数据库中存在两条 `name='生产汇聚交换机'` 的设备记录：
- `10.1.100.2`，`sys_name=DMZ-2-HJ`
- `10.8.0.102`，`sys_name=None`

它们可能是同一台堆叠/双管理口设备的不同 IP，被重复录入，导致拓扑图画出两个同名节点。

**修复方案**：
1. **自动采集设备自身 sysName**：`app/collector/topology_snmp.py` 新增 `collect_device_sysname()`，SNMP GET `1.3.6.1.2.1.1.5.0`。
2. **scheduler 更新 sys_name**：每次 SNMP 采集成功后，自动把设备 sysName 写回 `Device.sys_name`。
3. **拓扑 API 按 sys_name 合并节点**：`app/routers/topology.py` 的 `get_topology()` 对相同 `sys_name` 的设备记录合并为一个逻辑节点。
   - 主设备选择：堆叠/集群/M-LAG 类型优先，在线状态优先，创建时间早优先。
   - 合并后节点带 `aliases` 字段，列出被合并的别名 IP/名称。
4. **前端展示别名**：拓扑详情面板显示「别名/成员」IP 列表。

**改动文件**：
- `backend/app/collector/topology_snmp.py`：新增 `collect_device_sysname()`。
- `backend/app/collector/scheduler.py`：采集成功后更新 `device.sys_name`。
- `backend/app/routers/topology.py`：`get_topology()` 按 `sys_name` 合并节点。
- `frontend/src/api.ts`：`TopologyNode` 加 `aliases`。
- `frontend/src/pages/Topology.tsx`：详情面板展示别名/成员。

验证：把 `10.8.0.102` 的 `sys_name` 设为 `DMZ-2-HJ` 后调用 `get_topology()`，两个"生产汇聚交换机"合并为一个节点，别名包含 `10.1.100.2`。

### 4. 拓扑管理（从零开发，本次新增）
**自动发现**：LLDP（`LLDP-MIB` 1.0.8802.1.1.2.1.4.1.1）+ CDP（`CISCO-CDP-MIB` 1.3.6.1.4.1.9.9.23）WALK 邻居 → 解析对端 sysName/IP 匹配设备表 → 写 `link` 表。

**改动文件：**
- `backend/app/models.py`：新增 `Link` 模型（local/remote device_id、local/remote port、sysname、ip、protocol、link_type）。
- `backend/app/collector/topology_snmp.py`：**新增**。LLDP/CDP 采集。
- `backend/app/collector/topology.py`：**新增**。`discover_topology()` 遍历在线设备、写链路（每轮先清旧链路全量重算）。
- `backend/app/collector/scheduler.py`：采集完成后自动调用 `discover_topology_safe()`。
- `backend/app/routers/topology.py`：**新增**。`GET /api/topology` 返回节点(设备)+连线(含未录入对端的虚拟节点)。
- `backend/app/main.py`：挂载 `topology` 路由（**注意：第 13 行 import 列表需含 `topology`，曾遗漏导致 NameError，已修**）。
- `backend/app/db.py`：建 `link` 表迁移。
- `frontend/src/api.ts`：加 `TopologyNode/TopologyLink/TopologyData` + `fetchTopology()`。
- `frontend/src/pages/Topology.tsx`：**新增**。SVG 力导向图（自实现斥力+弹簧布局，无额外依赖），按状态着色、可拖拽、点击看详情、区域筛选。
- `frontend/src/components/Layout.tsx` + `App.tsx`：加「🔗 拓扑管理」入口 + `/topology` 路由。

**实测**：10.8.0.1 核心交换机发现 **35 条 LLDP 链路**（与 `dis lldp n b` 吻合）。

---

## 二、关键 Bug 与修复记录

1. **拓扑只发现 1 条链路（严重）**
   - 根因：`_snmp_walk` 只返回 OID **最后一段**索引（如 `3`），LLDP 三段索引 `(TimeMark.LocalPortNum.MSAPId)` 的前两段丢失，导致 35 条记录 key 冲突互相覆盖。
   - 修复：给 `_snmp_walk` 加 `return_full_oid=True` 参数；`topology_snmp.py` 用完整索引关联。**勿删此参数**，否则其他表格采集（接口表等）依赖旧行为。
2. **LLDP 端口乱码**：原用 `lldpRemLocalPortId(.7)`/`lldpRemPortId(.4)`（二进制），改为可读文本 `lldpRemLocalPortDesc(.8)` / `lldpRemPortDesc(.6)`。
3. **main.py 漏 import topology**：已补。

---

## 三、当前数据库状态（netops.db）
- 已自动创建默认规则：**CPU利用率过高**（阈值 85=P1，严重阈值 95=P0）、**内存利用率过高**（同上）。
- 已有 `link` 表数据（10.8.0.1 的 35 条 LLDP 链路）。
- 区域/子区域表已存在（需用户在「区域管理」页录入实际区域）。

---

## 四、明日待办 / 建议方向

优先级从高到低：

1. **重要链路标记功能**（P0 告警来源之一）—— ✅ 已完成（2026-08-26）
2. **拓扑图增强（全屏、布局、网元类型、sys_name 匹配）** —— ✅ 已完成（2026-08-26）

3. **链路标签与带宽可视化**
   - 在连线上显示本端/对端端口对；
   - 根据接口利用率给连线着色或粗细变化（绿色→黄色→红色）。

4. **告警闭环**
   - 离线告警恢复时，不应误关同设备阈值告警（scheduler 第 179 行左右 `resolve all active alerts for device` 需改为只处理 offline/error 类）。

5. **设备详情页**
   - 确认 CPU/内存曲线已正常（S7700 MIB 修复后）；
   - 接口列表、端口利用率。

6. **SNMP Trap 接收**（已有 `trap` 路由与模型）：验证 Trap → 告警联动。

---

## 五、启动与验证速记

```powershell
# 后端（管理员 PowerShell，因已有实例以管理员启动，需先结束占用 8000 的进程）
netstat -ano | findstr :8000
taskkill /PID <上面PID> /F
cd d:\Agent_NetOps\netops-platform\backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 前端（Vite HMR 已生效，改动自动热更；如需全量验证）
cd d:\Agent_NetOps\netops-platform\frontend
npm run dev
npm run build   # 验证类型，当前应干净通过
```

**验证拓扑**：后端启动后等 60s 采集一轮，访问「🔗 拓扑管理」应看到完整网络图。

---

## 六、关键配置/约定速查
- 默认管理员：`admin` / `admin123`
- 采集间隔默认 60s（`Device.poll_interval`，范围 10-3600）
- 设备凭据 AES 加密存 `device_credential` 表；密钥来自 `.env` 的 `CREDENTIALS_KEY`
- 指标存 VictoriaMetrics（需先启动，见旧文档第 1 步）
- SNMP 采集优先 v3，设备 `snmp_version` 默认 3

*关闭 CodeBuddy 不丢失文件/对话历史；本交接文档 + `NetOps平台开发进度.md` 为离线备份。*

---

## 七、2026-08-26 续：拓扑能力大幅增强（全天）

> 本节补充本日（2026-08-26）在拓扑模块当日完成的所有改动。**注意：本节部分内容修正/覆盖了上方第四节的旧描述**。

### 7.1 全屏改为浏览器原生全屏（真屏幕全屏）
- 旧文档写的是"网页内全屏"，实际已改为 `requestFullscreen()` 原生 API。
- `frontend/src/pages/Topology.tsx`：容器在 `isFullscreen` 时 `position:fixed; inset:0; zIndex:9999`，占满整个屏幕（非窗口内嵌）；全屏时隐藏右侧详情面板。

### 7.2 画布交互：滚轮缩放 + 左键拖拽平移
- 新增 `view` state（`tx/ty/k`），内容包在 `<g transform>` 里。
- `onWheel`：以光标为中心缩放（0.2x~4x）。
- `onCanvasDown/Move/Up`：左键按住空白拖拽平移；节点拖拽用 `stopPropagation` 不触发平移。
- 工具栏新增「🔍 重置视图」按钮（归位 tx/ty/k=0）。
- `svgPoint()` 已考虑 view 变换反算世界坐标。

### 7.3 堆叠/网元类型终于正确显示（数据层根因）
- **根因**：之前拓扑图全画成圆形，不是代码 bug，而是**数据库里三台已录入设备的 `device_type` 字段一开始就是 `single`**（录入时没选类型），API 忠实返回 single。
- **修复**：用户确认三台都是堆叠后，已将以下三台 `device_type` 改为 `stack`：
  - `10.1.100.2` / sysName=Core / S620（云汉核心）
  - `10.8.0.1` / sysName=core / S7706
  - `10.8.0.102` / DMZ-2-HJ（生产汇聚）
- 节点形状：堆叠=圆角矩形、M-LAG=菱形、集群=六边形、单机=圆形，名称下方加类型小字标签。

### 7.4 堆叠口自连过滤（解决"自己连自己"）
- **现象**：`DMZ-2-HJ` 通过堆叠业务口学到的 LLDP 邻居 sysName 就是自己，产生了 4 条"自连"假链路。
- **修复**：
  - `backend/app/collector/topology.py`：在 `discover_topology()` 邻居循环里加硬过滤——`remote_sysname == device.sys_name` 的邻居直接跳过（不依赖 local_port 是否取到）。
  - `backend/app/collector/topology_snmp.py`：新增 `STACK_PORT_PATTERN`（`Stack-Port`/`StackPort`/`Bridge-Aggregation`/`堆叠`/`CSS-Port`/`iStack` 等），辅助识别。
- 下次采集自动清除已有假链路（采集逻辑先清旧链路全量重算）。

### 7.5 聚合链路（LAG）识别与合并
- `topology_snmp.py`：新增 `LAG_PORT_PATTERN`（Eth-Trunk/Port-Channel/Aggregate 等），识别后标 `link_type='lag'`。
- `Link` 模型 / 建表已含 `link_type` 列；`discover_topology` 写入。
- 前端：同一对设备间的多条物理链路**自动合并为一条粗线**，标注 `LAG ×N`；图例新增「聚合链路 (LAG)」。
- 实测：DMZ-2-HJ 到 sw-server-B12-2 有 4 条、到 S6520-WSBusiness01 有 2 条等重复对，均被合并。

### 7.6 显示选项面板（用户可控显隐）
- 工具栏新增 **⚙ 显示** 按钮，弹出面板含 5 个开关：
  - 设备名称（默认开）、接口名称（默认关）、链路类型 LLDP/CDP（默认开）、区域（默认开）、子区域（默认关）。
- 后端 `get_topology()` 新增查询 `Region`/`SubRegion` 名字，node 返回 `region_name`/`sub_region_name`。
- 前端 `TopologyNode` 接口补 `region_name`/`sub_region_name` 字段；节点上按选项渲染区域（蓝）/子区域（浅蓝）小字。
- 修复记录：面板初版因父容器缺 `position:relative` 渲染到屏幕外，后改为按钮外包 `<span relative>` 紧贴按钮下方弹出。

### 7.7 自定义视图模式（保留拖拽位置）
- 布局按钮新增 **自定义**（区别于力导向/星型/树形）。
- 切到自定义模式：停止力导向动画，节点位置固定。
- 用户左键拖拽任意节点，拖拽结束 `onCanvasUp` 时把位置存入 `customPositionsRef`。
- 再次切回自定义模式恢复上次拖拽位置。

### 7.8 设备编辑页网元类型可下拉修改
- `frontend/src/pages/DeviceDetail.tsx`：详情头部"网元类型"从只读改为 `<select>`（单机/堆叠/M-LAG/集群），`onChange` 调 `updateDevice` 即时保存。
- 添加设备页（DeviceList.tsx）本就有该下拉。

### 7.9 Favicon
- `frontend/index.html` 加 `<link rel="icon" href="/logo.ico">`。
- `D:\NetOps\logo.ico` 复制到 `frontend/public/logo.ico`（Vite public 目录自动 serve 到根路径）。

### 7.10 云汉核心（10.1.100.2）LLDP 采不到的根因与修复
- **现象**：`10.1.100.2` CPU/内存能采（说明 SNMP 通），但 LLDP walk 返回 0 行，而 `10.8.0.102` 用同一 community 能采到 44 条。
- **根因**：华为 S 系列默认 SNMP view `ViewDefault` 只放行 `internet`(1.3.6.1) 子树，**不含 `1.0.8802`（LLDP-MIB 子树）**，所以 LLDP 读不到。MIB 文档 `01-155 LLDP-MIB.pdf` 指明需 `snmp-agent mib-view included` 放行。
- **设备上修复**（用户在云汉核心执行）：
  ```
  snmp-agent mib-view included NetOpsView internet
  snmp-agent mib-view included NetOpsView 1.0.8802
  snmp-agent community read Read102419 mib-view NetOpsView
  save
  ```
- **验证**：后端直接用 `_build_device_info` + `collect_lldp_neighbors` 对 `10.1.100.2` 实测，从 0 条 → **6 条**邻居（USG6500E、Access-1~4、S1）。

### 7.11 config.py 缩进崩溃修复（当日收尾）
- `app/config.py` 第 26 行残留 `"""  """` 空三引号字符串，导致 `collect_interval_seconds` 等字段缩进报错、后端 `IndentationError` 启动失败、前端 API 全部 502。
- 已删除该行，后端恢复正常 `import` 通过。

### 7.12 当日改动文件清单（汇总）
**后端**
- `app/config.py`：删残留字符串（修复崩溃）
- `app/collector/topology_snmp.py`：`STACK_PORT_PATTERN`、`LAG_PORT_PATTERN`、`collect_device_sysname`、邻居返回 `link_type`
- `app/collector/topology.py`：自连过滤、`link_type` 写入
- `app/routers/topology.py`：`region_name`/`sub_region_name` 查询返回
- `app/models.py` / `app/db.py`：`Link.link_type`（已建列）
- `app/main.py`：挂载 topology（此前已修正）

**前端**
- `frontend/index.html`：favicon
- `frontend/public/logo.ico`：图标文件
- `frontend/src/api.ts`：`TopologyNode` 加 `region_name`/`sub_region_name`
- `frontend/src/pages/Topology.tsx`：真全屏、滚轮缩放/拖拽平移、显示选项面板、自定义视图、LAG 合并、区域标注、堆叠类型标签
- `frontend/src/pages/DeviceDetail.tsx`：网元类型可编辑下拉

### 7.13 当日未做 / 遗留项
1. **链路标签带宽可视化**：连线上暂未显示本端/对端端口对、未做接口利用率着色（旧文档第四节第 3 点）。
2. **告警闭环**：离线告警恢复时可能误关阈值告警（旧文档第四节第 4 点），未处理。
3. **设备详情页接口列表/端口利用率**：未做。
4. **SNMP Trap 联动验证**：未验证。
5. **区域/子区域实际数据**：需在「区域管理」页录入真实区域（拓扑筛选依赖）。
6. **自定义视图位置持久化**：当前 `customPositionsRef` 仅内存保存，刷新页面后丢失；如需持久化需存后端/DB。

### 7.14 当日验证状态
- ✅ 后端 `import app.config` 通过
- ✅ `npm run build`（前端）干净通过
- ✅ 云汉核心 LLDP 实测 0→6 条邻居
- ✅ 三台设备 device_type=stack 已入库
- ⏳ 因当日收尾时后端曾崩溃，需**重启后端 + 刷新前端**确认所有前端改动（全屏/缩放/显示选项/自定义视图/区域标注）在浏览器实际生效。

---

## 八、明日建议（2026-08-27 起）
优先级从高到低：

1. **重启后端验证今日全部改动**：`netstat -ano | findstr :8000` → `taskkill` → 重新 `uvicorn app.main:app`，等 60s 采集，浏览器看拓扑全屏/缩放/显示选项/自定义视图/区域标注/堆叠矩形/DMZ-2-HJ 自连清除是否都生效。
2. **自定义视图持久化**：`customPositionsRef` 存 localStorage 或后端，刷新不丢。
3. **链路带宽可视化**：端口对标签 + 接口利用率着色。
4. **告警闭环修复**：离线恢复不误关阈值告警。
5. **设备详情接口列表/端口利用率**。
6. **SNMP Trap 接收验证**。

*关闭 CodeBuddy 不丢失文件/对话历史；本交接文档 + `NetOps平台开发进度.md` 为离线备份。*
